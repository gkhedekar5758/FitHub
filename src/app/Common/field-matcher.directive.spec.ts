import { FieldMatcherDirective } from './field-matcher.directive';

describe('FieldMatcherDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldMatcherDirective();
    expect(directive).toBeTruthy();
  });
});
